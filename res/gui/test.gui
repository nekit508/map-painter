func create #table {
    default {
        top()
        left()
    }

    label #label {
        setText("Yeah, it is fucking working!!")

        setWidth(125f)
    } [ right() left ]

    setFillParent(true)
}

func main {
    .create
}